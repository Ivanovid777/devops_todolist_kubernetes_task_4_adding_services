apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  namespace: todoapp
spec:
  type: NodePort
  selector:
    app: todolist
  ports:
    - protocol: TCP
      port: 91
      nodePort: 30777
      targetPort: 8080
