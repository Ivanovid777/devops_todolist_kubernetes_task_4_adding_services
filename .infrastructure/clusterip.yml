apiVersion: v1
kind: Service
metadata:
  name: clusterip-service
  namespace: todoapp
spec:
  type: ClusterIP
  selector:
    app: todolist
  ports:
    - protocol: TCP
      port: 90
      targetPort: 8080
